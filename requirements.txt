# Core audio
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24,<2

# Wake word
openwakeword>=0.6.0
onnxruntime>=1.15
speexdsp-ns>=0.1.2

# STT
pywhispercpp>=0.2.0
scipy>=1.10

# Voice activity detection (built-in, energy-based — no external dep)
# webrtcvad removed — C extension doesn't support Python 3.12

# Config
setuptools>=68.0
pydantic>=2.0
